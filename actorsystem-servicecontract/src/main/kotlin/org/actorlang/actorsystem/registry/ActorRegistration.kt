package org.actorlang.actorsystem.registry

/**
 * A memento used to represent an actor registration in a registry.
 * It is used a token so that only the user having registered an actor can unregister it.
 */
interface ActorRegistration
